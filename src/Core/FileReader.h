#ifndef FILE_READER_H
#define FILE_READER_H

//TODO: Remove these files if they are unused
char* LoadFile(const char* filePath);

	/*
	//for (int i = 0; i < 99; i++)
			{
				if (SDL_RWread(file, &data[0], 1, 112) < 0)
				{
					//break;
					return "";
				}
			}
}
std::string value;
for (int i = 0; i < 112; i++)
{
	value += data[i];
}

SDL_RWclose(file);
return value;

*/
#endif FILE_READER_H